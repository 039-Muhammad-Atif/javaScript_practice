Q#1. Create array of number and take input from the user to add number to the Array.
Q#2. Keep added the numbers to the array in Q#1. until 0 is added to the Array.
Q#3. Filter for a number which is divisible by 10 from a given array.
Q#4. Create an array of a square of a given number.
Q#5. Use reduced to calculate factorial of given number from an array of first "n" natural number
        (n being the number whose factorial needs to be calaulated)